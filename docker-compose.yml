services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: exchange
      POSTGRES_USER: user
      POSTGRES_PASSWORD: astra
    # ports:
    #   - "5432:5432"
    # volumes:
    #   - pgdata:/var/lib/postgresql/data

  app:
    build: .
    environment:
      POSTGRES_DB: exchange
      POSTGRES_USER: user
      POSTGRES_PASSWORD: astra
      POSTGRES_HOST: db
      POSTGRES_PORT: 5432
    links:
      - db:database
    ports: ["8080:8080"]
# volumes:
  # pgdata: 